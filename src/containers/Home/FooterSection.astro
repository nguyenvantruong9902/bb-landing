---
import { Image } from "astro:assets";

import DownloadOn from "@components/DownloadOn.astro";

import FooterBgImg from "@assets/images/footerBg.png";
import UserFooterImg from "@assets/images/home/userFooter.png";
---

<div class="max-w-[90rem] mx-auto mt-28 md:px-0 xl:mt-48 xl:px-20">
  <div class="wrapper">
    <div class="relative md:max-w-[22.625rem] lg:max-w-lg">
      <h3 class="text-h3 text-white font-semibold">
        Sẵn sàng cho trải nghiệm làm đẹp tuyệt vời?
      </h3>
      <p class="mt-4 text-title text-white text-opacity-semi">
        Download BeautyBooking và khám phá các dịch vụ làm đẹp tốt nhất. Nhanh
        chóng và dễ dàng, không cần lo lắng về việc đặt lịch nữa.
      </p>

      <div class="hidden md:block md:pb-12 xl:pb-20">
        <DownloadOn className="mt-20 gap-4 md:justify-start" />
      </div>
    </div>

    <div
      class="relative mt-8 md:absolute md:bottom-0 md:right-12 md:max-w-[22rem] xl:mt-0 xl:right-16 xl:max-w-[37.5rem]"
    >
      <Image src={UserFooterImg} alt="user-footer" />

      <div class="block md:hidden">
        <DownloadOn className="gap-4 md:justify-start" />
      </div>
    </div>
  </div>
</div>

<style define:vars={{ footerBgUrl: `url(${FooterBgImg.src})` }}>
  .wrapper {
    @apply relative rounded-t-[2rem] pt-12 pb-[3.25rem] px-6 md:px-12 md:pb-0 xl:pt-20 xl:px-20;

    background: linear-gradient(180deg, #7f55f1 0%, #5a39f0 100%);
  }

  .wrapper::before {
    background: var(--footerBgUrl) no-repeat;
    background-position: 50% 0;

    @apply content-[''] block absolute left-0 top-0 w-full h-full opacity-20 bg-cover rounded-t-[2rem];
  }
</style>
