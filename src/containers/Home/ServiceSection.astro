---
import { Image } from "astro:assets";

import SpaServiceImg from "@assets/images/spaService.png";
import MassageServiceImg from "@assets/images/massageService.png";
import HairSalonServiceImg from "@assets/images/hairSalonService.png";
import NailSalonServiceImg from "@assets/images/nailSalonService.png";
import BeautyClinicServiceImg from "@assets/images/nailSalonService.png";
import DentalClinicServiceImg from "@assets/images/dentalClinicService.png";

const serviceList = [
  {
    icon: SpaServiceImg,
    title: "Spa",
    description: "Thư giãn tinh thần",
  },
  {
    icon: MassageServiceImg,
    title: "Massage",
    description: "Chăm sóc cá nhân",
  },
  {
    icon: HairSalonServiceImg,
    title: "Hair Salon",
    description: "Mái tóc hoàn hảo",
  },
  {
    icon: NailSalonServiceImg,
    title: "Nail Salon",
    description: "“Look at my nails”",
  },
  {
    icon: BeautyClinicServiceImg,
    title: "Beauty Clinic",
    description: "Xinh đẹp rạng ngời",
  },
  {
    icon: DentalClinicServiceImg,
    title: "Dental Clinic",
    description: "Tự tin nụ cười",
  },
];
---

<div class="px-5 text-center mt-24 md:mx-20 md:mt-[9.56rem]">
  <div class="max-w-2xl mx-auto">
    <h1 class="text-h3 font-semibold text-opacity-pure md:text-h2">
      Tất cả các dịch vụ làm đẹp chỉ trong 1 ứng dụng
    </h1>

    <p class="text-title text-opacity-semi mt-4 mb-8 md:text-title md:mx-12">
      Bạn có thể tìm thấy tất cả các dịch vụ tại BeautyBooking, chỉ cần đó là
      dịch vụ làm đẹp và chăm sóc sức khỏe
    </p>
  </div>

  <div class="service-list">
    {
      serviceList.map(({ icon, title, description }) => (
        <div>
          <Image src={icon} alt="Spa Service" />
          <h2 class="text-title mt-4 mb-1 text-opacity-semi">{title}</h2>
          <p class="text-sub-header text-opacity-neutral">{description}</p>
        </div>
      ))
    }
  </div>
</div>

<style>
  .service-list {
    @apply grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 rounded-3xl p-8 shadow-md border-2 border-grey-300;
  }
  .service-list div {
    @apply flex flex-col justify-center items-center p-8 sm:border-r-2 border-grey-300;
  }

  .service-list div:nth-child(n + 2) {
    @apply border-t-2;
  }

  @media (min-width: 640px) {
    .service-list div:nth-child(n + 2) {
      @apply border-t-0;
    }

    .service-list div:nth-child(n + 3) {
      @apply border-t-2;
    }

    .service-list div:nth-child(2n) {
      @apply border-r-0;
    }
  }

  @media (min-width: 768px) {
    .service-list div:nth-child(n + 3) {
      @apply border-t-0;
    }

    .service-list div:nth-child(2n) {
      @apply border-r-2;
    }

    .service-list div:nth-child(n + 4) {
      @apply border-t-2;
    }

    .service-list div:nth-child(3n) {
      @apply border-r-0;
    }
  }
</style>
