---
import { Image } from "astro:assets";

import Container from "@components/Container.astro";

import PartnershipFooter from "@assets/images/partnershipFooter.png";
---

<Container className="mt-96 mx-5 md:mx-20">
  <div
    class="relative rounded-t-[2rem] py-20 px-6 md:px-20"
    style="background: linear-gradient(180deg, #7F55F1 0%, #5A39F0 100%);"
  >
    <div class="md:max-w-lg">
      <h3 class="text-h3 text-white font-semibold text-opacity-pure">
        Tham gia cộng đồng BeautyBooking
      </h3>
      <p class="mt-4 text-title text-white text-opacity-semi">
        Tham gia ngay hôm nay để tiếp cận nguồn khách hàng đa dạng & phát triển
        kinh doanh cơ sở của bạn.
      </p>

      <button
        class="mt-11 bg-white p-5 w-full rounded-xl text-title hover:bg-opacity-80 md:w-[25rem]"
      >
        Mở cửa hàng ngay
      </button>
    </div>

    <div class="hidden md:block absolute bottom-0 right-16 max-w-xl">
      <Image src={PartnershipFooter} alt="footer" />
    </div>
  </div>
</Container>
